spring:
  application:
    name: personal-diary-registration-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
  jpa:
    hibernate:
      ddl-auto: none
